---
import PublicLayout from '../../layouts/PublicLayout.astro';

export function getStaticPaths() {
  // Share pages require SSR to look up tokens dynamically.
  // No pages are generated at build time.
  return [];
}

const { token } = Astro.params;
---

<PublicLayout title="Shared Decision — Decision Loop">
  <div class="pt-8">
    <p class="text-neutral-500">Share page for token: {token}</p>
    <p class="mt-2 text-sm text-neutral-400">
      This feature requires server rendering. Enable SSR with an adapter to use share links.
    </p>
  </div>
</PublicLayout>
