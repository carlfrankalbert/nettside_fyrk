---
import ToolLayout from '../../layouts/ToolLayout.astro';
import PreMortemBrief from '../../components/PreMortemBrief';
import { preMortemTool } from '../../data/tools';

const { seo, toolName, breadcrumbLabel, h1, badge, subtitle, tagline, whenToUse, benefits, relatedTools, mobileCTA } = preMortemTool.page;
---

<ToolLayout
  title={seo.title}
  description={seo.description}
  toolName={toolName}
  breadcrumbLabel={breadcrumbLabel}
  whenToUse={whenToUse}
  benefits={benefits}
  relatedTools={relatedTools}
  mobileCTA={mobileCTA}
>
  <header slot="hero" class="mb-8" id="skriv">
    <h1 class="text-h3 md:text-h2 font-semibold text-brand-navy mb-3 leading-tight">
      {h1}
      <span class="inline-block text-xs font-medium text-amber-700 bg-amber-100 px-2 py-0.5 rounded-full align-middle ml-2">{badge}</span>
    </h1>
    <p class="text-base text-neutral-600 leading-relaxed mb-4">
      {subtitle}
    </p>

    <p class="text-xs text-neutral-500">
      {tagline}
    </p>
  </header>

  <PreMortemBrief client:load />
</ToolLayout>

<script>
  import { initPageViewTracking } from '../../scripts/pageview-tracking';
  import { initTrackingHelpers } from '../../scripts/tracking-exclusion';
  import { initMobileCTA } from '../../scripts/mobile-cta';
  initPageViewTracking('premortem');
  initTrackingHelpers();
  initMobileCTA({
    toolName: 'premortem',
    isDisabled: (d) => !!d.isLoading || !d.isValid,
    loadingText: 'Genererer brief...',
    defaultHtml: '<span>Generer Pre-Mortem Brief</span>',
  });
</script>
