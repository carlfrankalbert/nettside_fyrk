---
import '../styles/global.css';

const site = 'https://fyrk.no';
const title = 'Fyrk';
const description = 'Fyrk leverer produktledelse der kvalitet møter forretningsverdi.';
---

<!doctype html>
<html lang="no">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content={description} />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/fyrk-monogram-primary-navy.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>

    <!-- Open Graph -->
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
    <meta property="og:url" content={site} />
    <meta property="og:locale" content="nb_NO" />
    <meta property="og:site_name" content="Fyrk" />

    <!-- Twitter -->
    <meta name="twitter:card" content="summary" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    
    <!-- Canonical -->
    <link rel="canonical" href={site} />
  </head>
  <body>
    <div class="min-h-screen bg-gradient-to-b from-neutral-50 to-white flex items-center justify-center px-4">
      <div class="max-w-2xl mx-auto text-center flex flex-col items-center justify-center">
        <!-- Logo -->
        <div class="mb-8">
          <img 
            src="/fyrk-logo-primary-navy.svg" 
            alt="Fyrk logo" 
            class="h-16 md:h-20 mx-auto"
            width="200"
            height="56"
          />
        </div>
        
        <!-- Contact -->
        <div class="mb-8">
          <a 
            href="mailto:hei@fyrk.no" 
            class="text-2xl md:text-3xl font-medium text-brand-cyan-darker hover:text-brand-cyan transition-colors duration-fast font-primary focus-visible:ring-2 focus-visible:ring-brand-cyan focus-visible:ring-offset-2 rounded-md px-2 py-1 block"
            aria-label="Send e-post til hei@fyrk.no"
          >
            hei@fyrk.no
          </a>
        </div>
        
        <!-- Animated API Icon -->
        <div class="api-icon-container" aria-hidden="true">
          <svg 
            class="w-12 h-12 md:w-16 md:h-16 text-brand-cyan" 
            fill="none" 
            stroke="currentColor" 
            viewBox="0 0 24 24"
          >
            <path 
              stroke-linecap="round" 
              stroke-linejoin="round" 
              stroke-width="2" 
              d="M8 9l3 3-3 3m5 0h3M5 20h14a2 2 0 002-2V6a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"
            />
          </svg>
        </div>
      </div>
    </div>
    
    <style>
      .api-icon-container {
        animation: pulse 2s cubic-bezier(0.4, 0, 0.6, 1) infinite;
      }
      
      @keyframes pulse {
        0%, 100% {
          opacity: 1;
          transform: scale(1);
        }
        50% {
          opacity: 0.7;
          transform: scale(1.1);
        }
      }
      
      @media (prefers-reduced-motion: reduce) {
        .api-icon-container {
          animation: none;
        }
      }
    </style>
  </body>
</html>
