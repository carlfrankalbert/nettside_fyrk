---
import ToolLayout from '../layouts/ToolLayout.astro';
import PageView from '../components/analytics/PageView.astro';
import KonseptSpeil from '../components/KonseptSpeil';
import { konseptspeilTool } from '../data/tools';

const { seo, toolName, breadcrumbLabel, h1, subtitle, tagline, whenToUse, benefits, relatedTools, mobileCTA } = konseptspeilTool.page;
---

<ToolLayout
  title={seo.title}
  description={seo.description}
  toolName={toolName}
  breadcrumbLabel={breadcrumbLabel}
  whenToUse={whenToUse}
  benefits={benefits}
  relatedTools={relatedTools}
  mobileCTA={mobileCTA}
>
  <header slot="hero" class="mb-8" id="skriv">
    <h1 class="text-h3 md:text-h2 font-semibold text-brand-navy mb-3 leading-tight">
      {h1}
    </h1>
    <p class="text-base text-neutral-600 leading-relaxed mb-4">
      {subtitle}
    </p>

    <p class="text-xs text-neutral-500">
      {tagline}
    </p>
  </header>

  <KonseptSpeil client:load />
  <PageView pageId="konseptspeil" />
</ToolLayout>

<script>
  import { initMobileCTA } from '../scripts/mobile-cta';
  initMobileCTA({
    toolName: 'konseptspeil',
    isDisabled: (d) => !!d.isLoading || (d.length as number) < 50,
    loadingText: 'Speiler tankene dine\u2026',
    defaultHtml: '<span>Start speiling</span>',
  });
</script>
